// Stub for @/components/ShoppingCartDrawer (design-sync bundle only). The real
// drawer pulls in Stripe Elements, the shipping flow and Socket.IO — none of
// which belong in the design system. Navbar only needs it to mount as a child.
export default function ShoppingCartDrawer() {
  return null
}
