// Stub for @/components/NewsletterSubscribeModal (design-sync bundle only).
// Rendered as an inert child; the real modal isn't part of the design system.
export default function NewsletterSubscribeModal() {
  return null
}
