/**
 * Email service module index.
 * Re-exports all email functions from the main emailService for backward compatibility.
 * Future refactoring can split templates into individual files under this directory.
 */
module.exports = require('../emailService');
