/**
 * Stripe Connect Zod schemas — Change #1: stripe-connect-accounts
 *
 * Placeholder module — the current Stripe Connect admin endpoints don't
 * accept a request body (params-only). If a future endpoint adds body
 * validation, add its schema here.
 */
// eslint-disable-next-line no-unused-vars
const { z } = require('zod');

module.exports = {};
